export default async function handler(req, res) {
  console.log('req: ', req);

  //   const result = await checkLogin(req.body.accessToken);
  //   console.log('result: ', result);
  //   res.status(200).json(result);
}
